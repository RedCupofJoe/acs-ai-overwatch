{{- if .Values.mattermost.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mattermost-data
  {{- include "acs-ai-overwatch.argocdSyncWaveAnnotations" (dict "root" . "wave" "mattermostWorkloads") | nindent 2 }}
  namespace: {{ .Values.mattermost.namespace }}
  labels:
    app.kubernetes.io/name: mattermost
    app.kubernetes.io/component: server
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.mattermost.pvc.storageClassName | default (include "acs-ai-overwatch.storageClassName" .) }}
  resources:
    requests:
      storage: {{ .Values.mattermost.pvc.size }}
{{- end }}
