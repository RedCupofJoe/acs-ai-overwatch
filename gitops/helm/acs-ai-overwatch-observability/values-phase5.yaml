# Observability defaults match this overlay (chart enabled: true; main chart
# observability.agentInstrumentation.enabled: true). Keep this file as an explicit
# overlay if you split valueFiles in the Argo Application.
enabled: true
