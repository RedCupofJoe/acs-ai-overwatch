apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PoC: do not apply this overlay by default. Helm ClusterPolicy enables time-slicing
# on GPU 0 (nvidia.com/gpu.shared). See configs/README.md.
resources:
  - cluster-policy.yaml
