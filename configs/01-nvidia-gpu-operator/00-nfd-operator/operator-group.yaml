# Name matches gitops/helm/acs-ai-overwatch (values.accelerators.nfd.operatorGroup.name)
# so pre-GitOps apply and Argo do not create two OperatorGroups in openshift-nfd.
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openshift-nfd
  namespace: openshift-nfd
spec:
  targetNamespaces:
    - openshift-nfd
